# John Jevi dela Cruz — portfolio source

This directory contains the dependency-free source for the GitHub Pages portfolio.

## Build

Run from this directory:

```sh
node scripts/build.mjs
```

The build writes the complete static site to `dist/`. Shared styles and behaviour live under `static/assets/`; verified credential and social-preview images remain in the repository's existing `assets/images/` directory.
